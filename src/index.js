import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//create the element
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,//then display it
  document.getElementById('root')
);